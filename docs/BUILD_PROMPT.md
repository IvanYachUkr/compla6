Build the attached Compression Lab system into a working, installable first release in this chat. This is an implementation task, not a request for another proposal. Work autonomously, use your execution environment, and return a downloadable `compression-lab-release.zip` containing the source tree, built wheel if possible, tests, raw test/benchmark outputs, and concise installation/operation instructions.

Start with `START_HERE.md` and `DESIGN.md` in the attachment. `VERIFY_BUNDLE.py` verifies every supplied file. The design decisions are settled: one reusable Python experiment engine and CLI, a thin optional MCP server, shared workflow instructions, clean connectors for current Codex/Prime/ZCode, and a separate owner-controlled private evaluation boundary. No dashboard, cloud service, custom agent orchestrator, or mandatory multi-agent workflow. MCP, skills and hooks are implementation options serving the product; a reliable install and end-to-end workflow matter more than those labels.

The attached reference code is existing benchmark infrastructure, not previous LLM codec solutions. Reuse useful parsers, candidate registration, measurement arithmetic, baselines, and tests instead of needlessly recreating them. The 12 supplied public log objects and 35 valid-input fixtures are implementation/smoke inputs only. You may create synthetic private samples to test access controls and one-test state semantics. There are no real held-out inputs, hidden metrics, old solutions, account data, or full agent traces in this package. Do not retrieve missing corpus partitions or consult the user's other chats. Public technical/library documentation is allowed.

Make the public profile/baseline/register/evaluate/status/export loop real, with at least one native C/C++/Rust baseline candidate that builds and round-trips. Final research candidates may use arbitrary reversible algorithms; do not implement only a parameter-tuning DSL. Enforce byte-exact reconstruction, stream/directory parity, valid unusual content, sanitizers, bounded corruption tests, immutable digest checks, complete cost accounting, resource limits, and fresh-process single-worker timing. The 100 decimal MB/s floor is ENCODING ONLY; report decoder speed, binary size, dependencies and peak memory. All seven timing trials must be retained. Hardware measurements from your environment are provisional and must never be invented to satisfy the gate.

Make installation deployable: a package with exact tested dependency pins/hashes, a repeatable evaluator runtime/container recipe, `doctor`, idempotent connect/disconnect preserving unrelated config, a shared skill, one-command model-free acceptance demo, and a clear unsupported-platform policy. Certified execution can be Linux x86-64 for v1, while the CLI/MCP protocol and agent integration remain portable. Hooks should be optional convenience checks, with the engine as release authority. A same-user directory or hidden MCP command is not enough to protect private data. Implement and test the declared isolation boundary, fail closed when it is unavailable, and show the operator how to run it.

Target current harnesses. Prime Agent v0.9.2, released 2026-09-05, has been checksum-verified and installed locally; its native MCP CLI and Python `mcp.list_tools` / `mcp.call_tool` interface were verified. Do not base the product on the old 0.7.2 experiments or patch private harness internals. See `COMPATIBILITY.md` for source URLs and commands. Test a real MCP handshake and safe call, not just JSON that resembles MCP. If live Codex/Prime/ZCode clients are unavailable in your environment, test config preservation and protocol behavior and explicitly distinguish those from live client verification.

Keep the implementation concise and coherent. Complete the vertical slice and its release boundary before optional features. Use conventional codecs as ready-made controls; dictionary training is train-only and fixed costs are charged. Include deterministic/random budget controls so future research can attribute an LLM gain correctly. Preserve negative results and expose compact structured state so agents do not repeatedly ingest large logs. Freeze/test must be owner-only, durable across crashes, bound to the exact digest, and terminal on completion.

Return the actual release artifact and a short account of what ran, what passed, what remains unsupported, and exact commands for us to install and validate it on Linux. If a tool/environment limitation blocks a check, report the limitation accurately and still return the best executable implementation you can produce. Do not stop at design prose or ask routine questions.
