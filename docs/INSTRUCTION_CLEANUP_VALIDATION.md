# Historical instruction cleanup validation, 0.5.5

This retained report describes the predecessor. Its result links resolve in `handover/instruction-cleanup-2026-09-11/compression-lab`, not this release. Current validation is recorded in `STAGED_TIMING_VALIDATION.md`.

The new instruction/configuration and adapter paths are verified. The [machine-readable source receipt](../results/instruction-validation.json) preserves the exact validation boundary and wheel identity. Installed-runtime verification and preservation receipts are recorded separately in `handover/instruction-cleanup-2026-09-11/VERIFIED055.json` in the research repository.

The full suite exercised 336 cases with no skips: 333 passed initially. One old controller fixture omitted the card's required search budget; two real MCP clients correctly rejected a local wheel that still contained the previous source revision. The fixture was corrected and the wheel rebuilt. All 20 affected checks then passed, covering all three initial failures. The production source stayed unchanged throughout the full suite and correction checks. Both [the original run](../results/run-contract-suite/work/full-suite.json) and [the correction run](../results/run-contract-recheck/work/recheck.json) remain available; this is not represented as one uninterrupted green run.

Additional checks include 50 focused instruction/visibility/observer/client/accounting tests and 17 release-tool tests. They cover all eight whole/split, visible/hidden and open/from-scratch combinations, applicable MCP tools, configured completion, exact feedback/compare schemas, preserved child effort, hidden-baseline readiness, generated workbench hashes and overwrite refusal, hypothesis tampering, library-hash mismatches and consistent primary/strict cost differences. Real SDK calls exercised the client adapter against actual evaluator results.

A [fresh synthetic native check](../results/run-contract-native-02/work/benchmark/SUMMARY.json) used 966,144 bytes in two complete objects. It verified missing-hypothesis rejection, registration, all full correctness/diagnostic/rebuild/resource gates, seven encode and seven decode trials, accounting, feedback and exact hypothesis/source export. It is a smoke integration check: the tiny input does not satisfy the 100 MB/s floor and is not certified research performance. The compiled standard codec remains fully charged in this static-link canary; separate accounting tests verify deductions for hash-matched shared codec libraries. The failed first infrastructure attempt is preserved separately.

The wheel's 104 package files match the final source bytes, and its clean Python environment passes `pip check`. Native checks hold the host benchmark lock and run in a private mount/PID namespace; every validation process tree is drained before releasing the lock. These checks made zero research-model calls and did not rerun a frozen scientific result.

The instruction changes have not been shown to improve research outcomes. Hypothesis receipts establish recording before registration, not the time source editing began. End-to-end timing still requires reviewing the implementation path; declaring a timing operation does not establish that an encoder avoids precomputed representations. From-scratch tool restrictions prevent supplied recipe creation, while source/linkage review remains necessary before certifying independent implementation.

The pending Yelp input card preserves its exact 100,000,488 corpus bytes, resources, budgets and hidden comparisons while explicitly adopting the new hypothesis, timing and primary-score policies. It is prepared, not launched. Existing Text8 and completed GLM runs retain their original runtimes and evidence.
