Inspect the code deeply: understand every line you add or change and the complete path from input through preparation, encoding and reconstruction. Explain how the algorithm works and where it spends time and memory. Deliver clean, concise software with the best measured performance you can achieve under the objective and budget. Remove unnecessary branches, abstractions and duplicate work. Avoid needless allocations, copies and repeated scans; use efficient memory layout and bounded working sets. Profile bottlenecks and verify improvements on the target resources. Preserve the validation and error handling needed for reliable decoding.

Before a substantive experiment, record a short hypothesis, expected benefit and falsifier with record_hypothesis; put its experiment_id in candidate.json. Keep the algorithm spec concise and precise about the format, steps, assumptions and failure cases. Separate later observations from the original hypothesis. Cite primary sources for unfamiliar algorithms; a from-scratch implementation need not be a novel algorithm.

Account for every dataset-dependent operation under the sealed timing policy and disclose all required computation. Decode must reconstruct every byte using only the archive and charged decoder bundle. No model calls participate in the evaluated codec pipeline.

Use small, meaningful checks during development. Screens and quick runs guide search; the final result requires every lab exactness, independence, corruption, diagnostic, resource and reproducibility gate plus all seven timing trials. Add or repeat tests only for a concrete unresolved risk or changed behavior. Preserve failed attempts and noisy trials.

Use the remaining research budget to investigate mechanisms, inspect implementation costs and test concrete, falsifiable improvements to compression and speed. Meeting a minimum gate is the start of optimization, not a performance target. Reserve time for final validation, export and a usable software handoff with source, build instructions, format, supported modes and measured tradeoffs. Follow the configured completion rules; a failed attempt does not justify stopping while the host requires further research. Preserve the strongest fully validated fast result and the smallest correct alternative. Derive numbers from immutable results; report size, all required encoding-stage times, decode time, memory, failures, limitations and exact export paths.
