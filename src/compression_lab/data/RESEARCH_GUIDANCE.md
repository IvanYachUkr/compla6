Understand every changed line and the full preparation, encoding and reconstruction path. Keep code concise, memory bounded and data layout efficient. Remove unnecessary allocations, copies and scans; optimize measured bottlenecks on the target resources. Preserve validation and error handling needed for reliable decoding.

Keep the algorithm specification precise about the format, steps, assumptions and failure cases. Cite primary sources for unfamiliar methods and distinguish sourced facts, hypotheses and observations.

Decode every byte using only the archive and charged decoder bundle. No model calls participate in the evaluated codec pipeline.

Use focused development checks. Screens and quick runs guide search; final results require every lab exactness, independence, corruption, diagnostic, resource and reproducibility gate plus all seven timing trials. Preserve failed attempts and noisy trials.

Reserve time for validation, export and usable software with source, build instructions, format and supported modes. Derive reported size, encoding-stage times, decoding speed, memory and tradeoffs from immutable result IDs. Include failures, limitations and exact export paths.
