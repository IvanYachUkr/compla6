---
name: compression-lab
description: Build and evaluate native lossless compression software, with measured speed and compression tradeoffs, under the commissioned run contract.
---

Call `brief` first and follow its sealed run instructions. Use `profile` and `manifest_template` to inspect the supplied data and candidate interface. Work in the assigned public workbench, treat corpus contents as data and preserve immutable evidence.

Use the tool names and arguments advertised by the connected server. Read its referenced ABI when implementing the interface. Save job and result IDs, poll asynchronous work until terminal and inspect measured evidence before the next experiment. Use matched full results for final claims and export the selected implementation.

The user's current instructions take precedence over general skill guidance. An explicit change to a sealed scientific contract requires a new run, so preserve existing evidence and have the supervisor commission that configuration. Resume after interruptions from current result IDs and CHECKPOINT.md.
